name: CD

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11
          distribution: 'temurin'
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Build with Gradle
        run: ./gradlew build --info

      - name: Docker Build
        run: docker build -t ${{ secrets.DOCKERHUB_IMAGE_NAME }} --build-arg DB_URL=${{ secrets.DB_URL }} --build-arg DB_USERNAME=${{ secrets.DB_USERNAME }} --build-arg DB_PASSWORD=${{ secrets.DB_PASSWORD }} --build-arg REDIS_PORT=${{ secrets.REDIS_PORT }} --build-arg REDIS_HOST=${{ secrets.REDIS_HOST }} --build-arg SECRET_KEY=${{ secrets.SECRET_KEY }} --build-arg ACCESS_EXP=${{ secrets.ACCESS_EXP }} --build-arg REFERSH_EXP=${{ secrets.REFRESH_EXP }} --build-arg S3_ACCESS_KEY=${{ secrets.S3_ACCESS_KEY }} --build-arg S3_SECRET_KEY=${{ secrets.S3_SECRET_KEY }} --build-arg HEADER=${{ secrets.HEADER }} --build-arg PREFIX=${{ secrets.PREFIX }} --build-arg S3_BUCKET=${{ secrets.S3_BUCKET }} --build-arg S3_REGION=${{ secrets.S3_REGION }} .
      - name: Login to DockerHub

        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Push image
        run: docker push ${{ secrets.DOCKERHUB_IMAGE_NAME }}